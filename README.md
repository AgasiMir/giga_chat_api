# AI Chat API

Проект представляет собой асинхронный REST API для взаимодействия с GigaChat API с возможностью сохранения истории запросов в PostgreSQL.

## 🌟 Основные возможности

- Интеграция с GigaChat API для генерации ответов на текстовые запросы
- Сохранение истории запросов и ответов в PostgreSQL
- Получение истории запросов пользователя по IP-адресу
- Покрытие тестами 73% кода
- Docker Compose для легкого развертывания

## 🏗️ Архитектура

Проект построен на современной асинхронной архитектуре:

- **FastAPI** - основной фреймворк для создания API
- **SQLAlchemy 2.0** - асинхронная работа с базой данных
- **PostgreSQL** - основная база данных
- **GigaChat API** - внешний сервис для генерации ответов
- **Pydantic** - валидация данных
- **Pytest** - тестирование с 73% покрытием

### Структура проекта:
```
src/
├── main.py              # Точка входа приложения
├── giga_chat_client.py  # Клиент для работы с GigaChat API
├── schemas.py           # Pydantic схемы для валидации данных
├── models/              # Модели SQLAlchemy
├── repository/          # CRUD операции
├── routers/             # API роутеры
└── database/           # Конфигурация базы данных
```

## 🚀 Установка и запуск

### Требования:
- Python 3.12+
- Docker и Docker Compose (для запуска PostgreSQL)

### Локальная установка:

1. Клонируйте репозиторий:
```bash
git clone <repository-url>
```

2. Установите зависимости:
```bash
pip install -r requirements.txt
uv sync 
```

3. Создайте файл `.env` на основе `.env.example` и заполните его:
```env
# GigaChat API
GIGACHAT_API_KEY=ваш_api_ключ

# Database
DB_DRIVER=postgresql+asyncpg
DB_USER=ваш_username
DB_PASSWORD=ваш_password
DB_HOST=localhost
DB_PORT=5432
DB_NAME=ваш_db_name
```

4. Запустите PostgreSQL через Docker:
```bash
docker-compose up -d
```

5. Примените миграции:
```bash
alembic upgrade head
```

6. Запустите приложение:
```bash
uvicorn src.main:app --reload
```


## 🧪 Тестирование

Проект имеет покрытие тестами 73% кода:

```bash
# Запуск тестов
pytest

# Просмотр отчета о покрытии
# Откройте htmlcov/index.html в браузере
```

## 📚 API Endpoints

### Документация API
Проект автоматически генерирует интерактивную документацию API через Swagger UI. После запуска приложения документация доступна по адресу:
- Swagger UI: `http://localhost:8000/docs`
- ReDoc: `http://localhost:8000/redoc`

### Основные endpoints:

#### Requests
- `GET /requests/` - Получить историю запросов текущего пользователя
- `POST /requests/` - Создать новый запрос к GigaChat API

#### Handlers
- `GET /handlers/check_db` - Проверить подключение к базе данных

## 🛠️ Технологии

- [FastAPI](https://fastapi.tiangolo.com/) - современный, быстрый веб-фреймворк
- [SQLAlchemy 2.0](https://www.sqlalchemy.org/) - ORM для работы с базой данных
- [PostgreSQL](https://www.postgresql.org/) - реляционная база данных
- [GigaChat API](https://developers.sber.ru/docs/ru/gigachat) - API для генерации текста
- [Pydantic](https://docs.pydantic.dev/) - валидация данных
- [Pytest](https://docs.pytest.org/) - фреймворк для тестирования
- [Docker](https://www.docker.com/) - контейнеризация приложения
- [Alembic](https://alembic.sqlalchemy.org/) - миграции базы данных
